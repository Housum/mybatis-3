<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.apache.ibatis.example.UserMapper" >

    <resultMap id="userResultMap" type="org.apache.ibatis.example.User" >
        <id property="id" column="id" />
        <result property="user_name" column="userName" />
        <result property="pwd" column="pwd" />
        <result property="age" column="age" />
    </resultMap>

    <sql id="refId">
        id,user_name,pwd,age
    </sql>

    <select id="listAll" resultMap="userResultMap">
		select <include refid="refId"/> from users
	</select>

    <select id="dynamicTest" resultMap="userResultMap">
        select * from users where age = #{param1}
    </select>

    <insert id="insert">
        insert into users (user_name,pwd,age) values (
        #{user.userName}, #{user.pwd}, #{user.age}
        )
    </insert>

</mapper>
